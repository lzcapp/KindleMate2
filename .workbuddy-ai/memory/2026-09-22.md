# 2026-09-22

## 接入 Rider 内置 MCP Server（JetBrains MCP）

- 用户贴来一份 SSE 配置：`http://127.0.0.1:64482/sse`，header `IJ_MCP_SERVER_PROJECT_PATH=/Users/laurence/Coding/KindleMate2`。
- 探测确认端口由 **Rider**（PID 54662）监听，SSE 端点返回 200，服务是活的。
- 写入 `~/.workbuddy-ai/mcp.json`，server 名 `jetbrains`，`type: sse` + `url` + `headers` + `disabled: false`。
  （该文件此前不存在，属新建；注意不要动 `~/.workbuddy-ai/connectors/*/mcp.json`，那是自动生成的。）
- 通过反查 CLI bundle 确认了字段名（`headers` 而非 `staticHeaders`）和 MCP 安全审批机制：
  新 server 处于 `PENDING_APPROVAL`，需在连接器管理页"自定义连接器"里点 **Trust** 才会连上；
  审批记录在 `~/.workbuddy-ai/mcp-approvals.json`，key = `sha256(url.origin)::<serverName>`。
  **没有**代用户伪造审批记录 —— 安全门保留给用户点。
- 待用户操作：Trust 后 MCP 生效（可能需要重启会话）。

<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>KindleMate2.Devices.MacOS</RootNamespace>
    <AssemblyName>KindleMate2.Devices.MacOS</AssemblyName>
  </PropertyGroup>

  <!--
    macOS 的 Kindle 设备实现。现在只是一个薄壳:真正的逻辑(USB 大容量存储扫描 + 经 libmtp 的 MTP)
    移到了 KindleMate2.Devices.Posix,由 macOS 与 Linux 共用 —— 两端唯一差别是"卷挂在哪里"。

    为什么拆:Linux 设备支持落地时,若把 macOS 那份实现复制一遍,会出现"修一处忘一处";
    而 MTP 那半边(libmtp 的 P/Invoke)本来就与 macOS 无关 —— libmtp 是 Linux 上的库,
    只是在 macOS 上要靠第三方客户端而已。

    TFM 用中性的 net10.0:薄壳没有平台专有依赖,Linux/Windows 的 CI 作业也能编译它 ——
    这正合 build.yml「在 ubuntu / windows 上构建整个解决方案」的既有做法。
    实际装配由 Avalonia.csproj 里的 IsOSPlatform('OSX') 条件引用控制。
  -->
  <ItemGroup>
    <ProjectReference Include="..\KindleMate2.Application\KindleMate2.Application.csproj" />
    <ProjectReference Include="..\KindleMate2.Devices.Posix\KindleMate2.Devices.Posix.csproj" />
    <ProjectReference Include="..\KindleMate2.Shared\KindleMate2.Shared.csproj" />
  </ItemGroup>

</Project>
